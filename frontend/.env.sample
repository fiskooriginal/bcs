# Frontend Environment Variables
# Configure backend connection URLs

# API and WebSocket URLs for backend connection
# When running in Docker, these should point to localhost from the browser perspective
VITE_API_URL=http://localhost:8001/api
VITE_WS_URL=ws://localhost:8001/api

